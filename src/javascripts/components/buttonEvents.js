// const buttonEvents = (e) => {
//   $('button').click((e => {
//     const target = e.target.id;
//   }));

//   if (target === )
// };

// export default { buttonEvents };

// $('#btnHealthy').click( )
